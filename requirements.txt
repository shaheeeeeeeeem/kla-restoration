# Generated from the working environment (Windows 11, Python 3.14, CUDA 12.6).
# torch/torchvision are the CUDA 12.6 builds; install them from the PyTorch index:
#   pip install torch==2.12.0 torchvision==0.27.0 --index-url https://download.pytorch.org/whl/cu126
# CPU-only machines can drop the index-url and use the default CPU wheels.
torch==2.12.0
torchvision==0.27.0
numpy==2.4.4
scipy==1.17.1
scikit-image==0.26.0
lpips==0.1.4
PyYAML==6.0.3
matplotlib==3.10.8
pandas==3.0.3
tqdm==4.67.3
